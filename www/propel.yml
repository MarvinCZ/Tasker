propel:
    database:
        connections:
            default:
                adapter: mysql
                dsn: mysql:host=localhost;dbname=tasker_development
                user: root
                password: mojeheslo
                settings:
                    charset: utf8
